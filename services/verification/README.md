# Verification service

Input: validated decision and the exact retrieved chunks. Output: acceptance or explicit errors.
Respond decisions below the tenant confidence threshold, with unknown chunk IDs, or with non-exact
quotes are rejected and converted to human escalation.

