# Security policy

Do not report vulnerabilities through public issues. Until a private disclosure channel is
configured, deployment outside an isolated development environment is prohibited.

Never commit model keys, database credentials, identity secrets, ticket content, or production
telemetry. LM Studio is a network endpoint and must be authenticated and protected by network policy
when it is not bound exclusively to loopback.

