# Infrastructure

Terraform starts after the cloud, regions, queue, secret manager, identity broker, RPO, and RTO are
approved. No production resources should be created manually. Required topology is documented in
`docs/architecture.md`.

